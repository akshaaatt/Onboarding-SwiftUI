import XCTest
@testable import Onboarding_SwiftUI

final class Onboarding_SwiftUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Onboarding_SwiftUI().text, "Hello, World!")
    }
}
